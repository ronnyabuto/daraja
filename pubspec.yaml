name: daraja
description: Full M-Pesa STK Push lifecycle for Flutter — Appwrite + Daraja 3.0.
version: 0.1.0
homepage: https://github.com/nyabuto/daraja

environment:
  sdk: '>=3.11.0 <4.0.0'
  flutter: '>=3.41.0'

dependencies:
  flutter:
    sdk: flutter
  appwrite: ^23.0.0
  http: ^1.3.0
  shared_preferences: ^2.5.5
  flutter_riverpod: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.4
  fake_async: ^1.3.2
