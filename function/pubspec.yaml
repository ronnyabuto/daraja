name: daraja_callback
description: Appwrite Function — M-Pesa STK Push callback handler for the daraja package.
version: 1.0.0

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  dart_appwrite: ^22.0.0
